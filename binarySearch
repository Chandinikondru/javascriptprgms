function binsearch(ele,arr,low,high)
{
    var mid=(low+high)/2;
    if(a[mid]==ele)
      return mid+1;
    else if(a[mid]>ele)
     return binsearch(ele,arr,low,mid-1);
     else
     return binsearch(ele,arr,mid+1,high);
     
     
    return -1;
 }
